# Lite-output, low-batch-size configuration for ICARUS, collection-only
# Revision date: June 25, 2025

include:
  - ../full_chain_co_250625.yaml
  - ../modifier/data/mod_data_250303.yaml
  - ../modifier/lite/mod_lite_240719.yaml

override:
  # Exists for legacy reasons, this is trivial enough to use CLI arguments
  io.loader.batch_size: 1
